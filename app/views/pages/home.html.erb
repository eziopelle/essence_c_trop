<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= image_path('home_banner.jpg') %>)">
  <h1>L'essence, c'est trop ! <strong>Passez à la location</strong> de <strong>voitures électriques</strong>!</h1>
  <p>Louer des voitures électriques pour l'environnement</p>
  <%# <a class="btn btn-primary" href="/cars">Accédez aux voitures</a> %>
  <div class="container">
    <%= simple_form_for :search, url: cars_path, method: :get do |f| %>
    <div class="search-form-control form-group">
      <input class="form-control string required" type="text" name="query" id="search_query" />
      <button name="button" type="submit" class="btn btn-flat">
      <i class="fas fa-search"></i> Rechercher

      </button>
    </div>
  </div>
  <% end %>
  <%= image_tag 'tesla.png', class: 'img-tesla' %>
</div>
